version: 2.1
orbs:
  node: circleci/node@1.1.6
executors:
  executor:
    docker:
      - image: "circleci/node:14.17.0"
jobs:
  test:
    executor: executor
    steps:
      - checkout
      - run: npm install
      - run: npm test
  build:
    executor: executor
    steps:
      - checkout
      - run: npm install
      - run: npm run build
  deploy:
    executor: executor
    steps:
      - checkout
      - run: npm install
      - run: npm run deploy
workflows:
  test_and_build:
    jobs:
      - test
      - build
      - deploy:
          requires:
            - test
            - build
          filters:
            branches:
              only: main
